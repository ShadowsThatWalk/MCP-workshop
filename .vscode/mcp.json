{
"inputs": [
  // The "inputs" section defines the inputs required for the MCP server configuration.
  {
    "type": "promptString"
  }
],
"servers": {
  // The "servers" section defines the MCP servers you want to use.
  "obsidian-kanban-tools": {
    "command": "uv",
    "args": [
      "--directory",
      "${workspaceFolder}",
      "run",
      "MCP_Server/server.py",
    ]
  }
 }
}